<div class="location">
  <h1><%= @place.city %>, <%= @place.state %></h1>
  <h2><%= @date["hourly_forecast"][0]["FCTTIME"]["weekday_name"] %> Morning, 9AM:</h2>
  <h3>Normal high: <%= @destination["almanac"]["temp_high"]["normal"]["F"] %>&#8457 / <%= @destination["almanac"]["temp_high"]["normal"]["C"] %>&#8451</h3>
  <h2><%= @date["hourly_forecast"][0]["FCTTIME"]["weekday_name_night"] %>, 9PM:</h2>
  <h3>Normal low: <%= @destination["almanac"]["temp_low"]["normal"]["F"] %>&#8457 / <%= @destination["almanac"]["temp_low"]["normal"]["C"] %>&#8451</h3>
  <h3>On this date, conditions have historically been listed as <b><%= @date["hourly_forecast"][0]["condition"].downcase %></b></h3>
  <img src="https://maps.googleapis.com/maps/api/staticmap?center=<%= @place.city %>,<%= @place.state %>&zoom=12&size=400x400&key=#{ENV['GMAPS_API_KEY']}">
</div>
<hr>
<div class="recommendations">
  <div>
    <h1>Suitcase Recommends</h1>
  </div>
  <div>
    <% @current_user.select_wardrobe(76).each do |item| %>
      <h4><%= item.name %></h4>
    <% end %>
  </div>
</div>